¡Hola\! Soy un experto en **TypeScript** y **JavaScript**. El error que estás viendo es exactamente el **comportamiento esperado** del código que has mostrado.

El error `Error: Esto es una prueba del tipo never` no es un problema; es la **consecuencia directa** de la función que has definido y llamado.

-----

## 🤯 Explicación del Comportamiento

El código fuente muestra una función en **TypeScript** (aunque luego se ejecuta como JavaScript, donde el *tipo* `never` se ignora, pero el *cuerpo* de la función sí se ejecuta):

```typescript
// Caso de error
function error(mensaje: string): never {
    throw new Error(mensaje);
}

error('Esto es una prueba del tipo never');
console.log('-------');
```

1.  **Función `error(mensaje: string): never`:**

      * La clave está en el tipo de retorno **`: never`**. En TypeScript, el tipo `never` se asigna a funciones que **nunca devuelven un valor** y **nunca terminan su ejecución** de forma normal.
      * La única manera de que una función no termine de forma normal es mediante un **bucle infinito** o, como en este caso, **lanzando una excepción** (`throw new Error(...)`).

2.  **Llamada `error('Esto es una prueba del tipo never')`:**

      * Cuando se llama a esta función, el código dentro del cuerpo (`throw new Error(mensaje);`) se ejecuta **inmediatamente**.
      * Esto hace que se **lance un nuevo objeto `Error`** con el mensaje `'Esto es una prueba del tipo never'`.

3.  **Resultado - El *Stack Trace*:**

      * Una vez que se lanza la excepción, el entorno de ejecución (en este caso, **Node.js**) detiene la ejecución del programa y muestra el error y la **pila de llamadas** (*stack trace*) para indicar dónde ocurrió el fallo.
      * El mensaje `Error: Esto es una prueba del tipo never` es el mensaje que tú mismo definiste en tu función.

### 🛑 ¿Por qué no se ejecuta `console.log`?

Debido a que la función `error` lanza una excepción (tipo `never`), el programa **termina abruptamente**. La línea siguiente, `console.log('-------');`, **nunca llega a ejecutarse**.

-----

## 🧑‍💻 El Tipo `never` en TypeScript

El tipo `never` es muy útil en TypeScript para:

  * **Garantizar la Exhaustividad:** Asegurar que se manejan todos los casos posibles en una construcción `switch`, obligando al desarrollador a manejar un nuevo caso si se añade un nuevo valor a un *tipo de unión* (Union Type).
  * **Documentar Funciones que Fallan:** Como en tu ejemplo, para declarar explícitamente que una función está diseñada para terminar el proceso (lanzando o saliendo).

En resumen, el error que ves **confirma que tu función funciona exactamente como fue diseñada** por el tipo de retorno `never`: **detiene la ejecución del programa** lanzando un error.

¿Te gustaría ver un ejemplo de cómo usar el tipo `never` para **comprobaciones de exhaustividad** en un bloque `switch` con *Union Types*?


CASO DE COMPILACIÓN (TRANSPILACIÓN)
npx tsc

CASO DE INTERPREACIÓN
npx ts-node archivo.ts